global:
  scrape_interval: 15s

rule_files:
  - /etc/prometheus/agentic-recording-rules.yml

scrape_configs:
  - job_name: agentic-orchestrators
    metrics_path: /actuator/prometheus
    static_configs:
      - targets:
          - orchestrator-a:8080
          - orchestrator-b:8080
