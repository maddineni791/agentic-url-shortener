package com.assessment.agentic.persistence;

public enum TaskStatus {
    PENDING,
    READY,
    CLAIMED,
    RUNNING,
    WAITING,
    SUCCEEDED,
    FAILED,
    REJECTED,
    CANCELLED,
    SAFE_STOPPED
}
